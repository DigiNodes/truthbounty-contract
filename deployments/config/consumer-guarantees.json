{
  "manifestVersion": 1,
  "issue": "V2-SC-134",
  "specification": "TruthBounty Chain Reorganization Consumer Guarantees (docs/reorg-consumer-guarantees.md)",
  "chainId": 11155420,
  "environment": "testnet",
  "consumerGuarantees": {
    "confirmationDepth": 1800,
    "maxFinalityClass": 2,
    "maxReorgDepth": 12,
    "eventsAreReplayable": true,
    "eventKeysAreUnique": true,
    "eventsAreTerminalOnEmission": true
  },
  "finalityClassVocabulary": {
    "None": 0,
    "SoftConfirmation": 1,
    "ProtocolFinalized": 2
  },
  "canonicalEventIdentity": {
    "keyFormula": "keccak256(abi.encode(chainId, contractAddress, blockNumber, blockHash, transactionHash, logIndex))",
    "ordering": ["blockNumber", "transactionIndex", "logIndex"],
    "removalRule": "a removal is lawful only when the removed key equals the key recomputed from the canonical coordinates of the orphaned log",
    "replayRule": "re-running ingestion from genesis over canonical logs must reproduce the identical key set"
  },
  "anchor": {
    "artifact": "ConsumerGuaranteesAnchor",
    "source": "contracts/v2/ConsumerGuaranteesAnchor.sol",
    "deployTimePublicationEvent": "ConsumerGuaranteesPublished",
    "authority": "read-only; no settlement, treasury, or outcome authority is exposed or granted"
  },
  "consumerResponsibilities": [
    "ingest only after confirmationDepth ancestor blocks are observed",
    "treat ProtocolFinalized windows as the only irreversible trigger",
    "absorb reorgs up to maxReorgDepth via lawful removals; resynchronize from genesis beyond it",
    "replay from genesis and compare the key set against the stored projection",
    "record chainId and anchor address at ingestion; never reuse guarantees across deployments"
  ]
}

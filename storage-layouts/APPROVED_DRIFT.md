# Approved Storage-Layout Drift (V2-SC-121)

Each line records one maintainer-approved layout change. The checker treats the
recorded `canonicalHash` as an approved target layout for the named contract.

Format (exact):

```
- `<Contract>` → `0x<64 hex lowercase>` (<PR link>, <classification>)
```

Entries:

- (none yet — initial freeze performed by the V2-SC-121 PR itself)
